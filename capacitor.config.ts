import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.drfendiameen.sunoarch',
  appName: 'Suno Architect',
  webDir: 'dist'
};

export default config;
